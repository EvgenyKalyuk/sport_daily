# Frontend проекта Sport Daily

> 📋 **Рекомендуемая структура проекта**: См. [STRUCTURE.md](./STRUCTURE.md) для детального описания рекомендуемой архитектуры на основе Feature-Sliced Design.
> 
> 📚 **Подробнее о FSD**: См. [FSD_GUIDE.md](./FSD_GUIDE.md) для полного руководства по Feature-Sliced Design.
> 
> 📋 **Шпаргалка FSD**: См. [FSD_CHEATSHEET.md](./FSD_CHEATSHEET.md) для быстрой справки по FSD.
> 
> 💡 **Примеры кода**: См. [STRUCTURE_EXAMPLES.md](./STRUCTURE_EXAMPLES.md) для примеров реализации базовых файлов.

## Текущая структура проекта

```
frontend/
├── public/                 # Статические файлы
│   └── favicon.png        # Иконка сайта
│
├── src/                    # Исходный код приложения
│   ├── app/               # Модули приложения
│   │   └── what.js        # Временный файл
│   │
│   ├── store/             # Redux store (управление состоянием)
│   │   ├── index.ts       # Конфигурация store
│   │   ├── hooks.ts       # Типизированные хуки для работы с Redux
│   │   └── slices/        # Redux slices (модули состояния)
│   │
│   ├── App.tsx            # Главный компонент приложения
│   ├── App.css            # Стили для App компонента
│   ├── index.tsx          # Точка входа приложения
│   └── env.d.ts           # TypeScript определения для переменных окружения
│
├── biome.json             # Конфигурация Biome (линтер и форматтер)
├── package-lock.json       # Lock файл зависимостей npm
├── package.json           # Зависимости и скрипты проекта
├── postcss.config.mjs     # Конфигурация PostCSS (для Tailwind CSS)
├── rsbuild.config.ts      # Конфигурация Rsbuild (сборщик)
└── tsconfig.json          # Конфигурация TypeScript
```

## Описание директорий и файлов

### `/public`
Статические файлы, которые копируются в корень собранного приложения.

### `/src`
Основная директория с исходным кодом приложения.

#### `/src/app`
Модули и страницы приложения. Здесь размещаются компоненты, специфичные для конкретных разделов приложения.

#### `/src/store`
Управление глобальным состоянием приложения через Redux Toolkit:
- `index.ts` - создание и экспорт store
- `hooks.ts` - типизированные хуки `useAppDispatch` и `useAppSelector`
- `slices/` - модули состояния (reducers и actions)

#### `/src/App.tsx`
Корневой компонент приложения, обычно содержит роутинг и общую структуру.

#### `/src/index.tsx`
Точка входа приложения. Здесь происходит рендеринг корневого компонента в DOM.

#### `/src/env.d.ts`
TypeScript определения для переменных окружения и глобальных типов.

### Конфигурационные файлы

#### `package.json`
Зависимости проекта:
- **React 19** - библиотека для UI
- **Redux Toolkit** - управление состоянием
- **React Router DOM** - маршрутизация
- **Recharts** - графики и визуализация данных
- **Lucide React** - иконки
- **date-fns** - работа с датами
- **Tailwind CSS** - утилитарный CSS фреймворк
- **Rsbuild** - сборщик на базе Rspack

#### `rsbuild.config.ts`
Конфигурация сборщика:
- Плагины для React и Babel
- Алиасы путей (`@/`, `@/store`, `@/components`)
- React Compiler через Babel плагин

#### `tsconfig.json`
Настройки TypeScript:
- Строгий режим проверки типов
- Поддержка ESNext модулей
- Алиасы путей для импортов
- Целевая версия ES2020

#### `biome.json`
Конфигурация линтера и форматтера Biome:
- Строгие правила для качества кода
- Автоформатирование с табами
- Организация импортов

#### `postcss.config.mjs`
Конфигурация PostCSS для обработки Tailwind CSS.

## Алиасы путей

В проекте настроены следующие алиасы (см. `rsbuild.config.ts` и `tsconfig.json`):

- `@/` → `./src/`
- `@/app` → `./src/app`
- `@/features` → `./src/features`
- `@/shared` → `./src/shared`
- `@/pages` → `./src/pages`
- `@/widgets` → `./src/widgets`
- `@/assets` → `./src/assets`

Пример использования:
```typescript
import { Button } from '@/shared/components/ui/Button';
import { WorkoutList } from '@/features/workouts/components/WorkoutList';
import { HomePage } from '@/pages/HomePage';
```

## Команды

- `npm run dev` - запуск dev сервера с автоперезагрузкой
- `npm run build` - сборка для production
- `npm run preview` - предпросмотр production сборки локально

## Технологический стек

- **React 19** - UI библиотека
- **TypeScript** - типизация
- **Redux Toolkit** - состояние
- **React Router** - маршрутизация
- **Tailwind CSS** - стили
- **Rsbuild** - сборка
- **Biome** - линтинг и форматирование
- **npm** - менеджер пакетов
