# Рекомендуемая структура фронтенд проекта

> 📚 **Подробнее о FSD**: См. [FSD_GUIDE.md](./FSD_GUIDE.md) для детального объяснения Feature-Sliced Design
> 
> 📋 **Шпаргалка**: См. [FSD_CHEATSHEET.md](./FSD_CHEATSHEET.md) для быстрой справки

## Общая структура

Структура основана на **Feature-Sliced Design (FSD)** — архитектурной методологии для организации фронтенд проектов.

```
frontend/
├── public/                    # Статические файлы
│   ├── favicon.png
│   ├── images/               # Изображения
│   └── fonts/                # Шрифты (если используются кастомные)
│
├── src/
│   ├── app/                  # Конфигурация приложения
│   │   ├── store.ts          # Redux store
│   │   ├── router.tsx        # Конфигурация роутинга
│   │   └── providers.tsx     # Обёртка с провайдерами (Redux, Router и т.д.)
│   │
│   ├── features/             # Функциональные модули (Feature-Sliced Design)
│   │   ├── auth/             # Модуль аутентификации
│   │   │   ├── api/          # API запросы
│   │   │   ├── components/   # Компоненты модуля
│   │   │   ├── hooks/        # Хуки модуля
│   │   │   ├── store/        # Redux slice для модуля
│   │   │   └── types.ts      # Типы модуля
│   │   │
│   │   ├── workouts/         # Модуль тренировок
│   │   │   ├── api/
│   │   │   ├── components/
│   │   │   │   ├── WorkoutList/
│   │   │   │   ├── WorkoutCard/
│   │   │   │   └── WorkoutForm/
│   │   │   ├── hooks/
│   │   │   ├── store/
│   │   │   └── types.ts
│   │   │
│   │   ├── statistics/       # Модуль статистики
│   │   │   ├── api/
│   │   │   ├── components/
│   │   │   ├── hooks/
│   │   │   ├── store/
│   │   │   └── types.ts
│   │   │
│   │   └── profile/          # Модуль профиля
│   │       ├── api/
│   │       ├── components/
│   │       ├── hooks/
│   │       ├── store/
│   │       └── types.ts
│   │
│   ├── shared/               # Переиспользуемые модули
│   │   ├── api/              # Базовые API утилиты
│   │   │   ├── baseApi.ts   # RTK Query base API
│   │   │   └── types.ts      # Общие типы API
│   │   │
│   │   ├── components/       # UI компоненты
│   │   │   ├── ui/           # Базовые UI компоненты
│   │   │   │   ├── Button/
│   │   │   │   ├── Button.tsx
│   │   │   │   ├── Button.module.css (или Button.css)
│   │   │   │   └── index.ts
│   │   │   │   ├── Input/
│   │   │   │   ├── Card/
│   │   │   │   ├── Modal/
│   │   │   │   ├── Select/
│   │   │   │   └── ...
│   │   │   │
│   │   │   ├── layout/       # Компоненты layout
│   │   │   │   ├── Header/
│   │   │   │   ├── Footer/
│   │   │   │   ├── Sidebar/
│   │   │   │   └── Layout.tsx
│   │   │   │
│   │   │   └── feedback/     # Компоненты обратной связи
│   │   │       ├── Toast/
│   │   │       ├── Loading/
│   │   │       └── ErrorBoundary/
│   │   │
│   │   ├── hooks/            # Переиспользуемые хуки
│   │   │   ├── useDebounce.ts
│   │   │   ├── useLocalStorage.ts
│   │   │   ├── useMediaQuery.ts
│   │   │   └── ...
│   │   │
│   │   ├── lib/              # Утилиты и хелперы
│   │   │   ├── utils.ts      # Общие утилиты
│   │   │   ├── format.ts     # Форматирование данных
│   │   │   ├── validation.ts # Валидация
│   │   │   └── constants.ts  # Константы
│   │   │
│   │   ├── store/            # Общие store утилиты
│   │   │   ├── hooks.ts      # Типизированные хуки
│   │   │   └── types.ts      # Общие типы store
│   │   │
│   │   └── types/            # Глобальные типы
│   │       └── index.ts
│   │
│   ├── pages/                # Страницы приложения
│   │   ├── HomePage/
│   │   │   ├── HomePage.tsx
│   │   │   └── index.ts
│   │   ├── WorkoutsPage/
│   │   ├── StatisticsPage/
│   │   ├── ProfilePage/
│   │   ├── LoginPage/
│   │   └── NotFoundPage/
│   │
│   ├── widgets/              # Составные виджеты (опционально)
│   │   ├── WorkoutCalendar/
│   │   ├── StatsDashboard/
│   │   └── ...
│   │
│   ├── assets/               # Ресурсы
│   │   ├── images/
│   │   ├── icons/
│   │   └── styles/
│   │       └── globals.css   # Глобальные стили
│   │
│   ├── App.tsx               # Корневой компонент
│   └── main.tsx             # Точка входа
│
├── biome.json
├── package.json
├── postcss.config.mjs
├── rsbuild.config.ts
├── tsconfig.json
└── README.md
```

## Принципы организации

### 1. Feature-Sliced Design (FSD)
Структура основана на принципах FSD:
- **features/** - бизнес-логика, разбитая по доменам
- **shared/** - переиспользуемый код
- **pages/** - страницы приложения
- **widgets/** - сложные составные компоненты (опционально)

### 2. Изоляция модулей
Каждый feature модуль содержит всё необходимое:
- `api/` - запросы к серверу
- `components/` - компоненты модуля
- `hooks/` - хуки модуля
- `store/` - Redux slice
- `types.ts` - типы модуля

### 3. Переиспользование через shared
- `shared/components/ui/` - базовые UI компоненты
- `shared/hooks/` - общие хуки
- `shared/lib/` - утилиты
- `shared/api/` - базовые API настройки

## Детальное описание директорий

### `/src/app`
Конфигурация приложения:
- `store.ts` - создание Redux store
- `router.tsx` - конфигурация React Router
- `providers.tsx` - обёртка с провайдерами (Redux Provider, Router и т.д.)

### `/src/features`
Бизнес-логика приложения, разбитая по доменам. Каждый feature:
- Самодостаточен (содержит всё необходимое)
- Изолирован от других features
- Может быть легко удалён или перемещён

**Пример структуры feature:**
```
features/workouts/
├── api/
│   └── workoutsApi.ts       # RTK Query endpoints
├── components/
│   ├── WorkoutList/
│   │   ├── WorkoutList.tsx
│   │   └── index.ts
│   └── WorkoutCard/
├── hooks/
│   └── useWorkouts.ts
├── store/
│   └── workoutsSlice.ts
└── types.ts
```

### `/src/shared`
Переиспользуемый код, не зависящий от бизнес-логики:

#### `shared/components/ui/`
Базовые UI компоненты (Button, Input, Card и т.д.):
- Каждый компонент в своей папке
- Экспорт через `index.ts`
- Стили рядом с компонентом

#### `shared/components/layout/`
Компоненты для структуры страниц (Header, Footer, Sidebar).

#### `shared/hooks/`
Переиспользуемые хуки (useDebounce, useLocalStorage и т.д.).

#### `shared/lib/`
Утилиты и хелперы:
- `utils.ts` - общие функции
- `format.ts` - форматирование (даты, числа, текст)
- `validation.ts` - функции валидации
- `constants.ts` - константы приложения

#### `shared/api/`
Базовые настройки API:
- `baseApi.ts` - RTK Query base API с настройками
- `types.ts` - общие типы для API

### `/src/pages`
Страницы приложения. Каждая страница:
- Компонент страницы
- Может использовать features и shared компоненты
- Экспорт через `index.ts`

### `/src/widgets` (опционально)
Сложные составные компоненты, объединяющие несколько features:
- Используется для очень сложных виджетов
- Можно пропустить на начальном этапе

### `/src/assets`
Статические ресурсы:
- `images/` - изображения
- `icons/` - иконки (если не используете lucide-react)
- `styles/globals.css` - глобальные стили

## Примеры импортов

### Импорт из shared
```typescript
import { Button } from '@/shared/components/ui/Button';
import { useDebounce } from '@/shared/hooks/useDebounce';
import { formatDate } from '@/shared/lib/format';
```

### Импорт из features
```typescript
import { WorkoutList } from '@/features/workouts/components/WorkoutList';
import { useWorkouts } from '@/features/workouts/hooks/useWorkouts';
import { workoutsApi } from '@/features/workouts/api/workoutsApi';
```

### Импорт страниц
```typescript
import { HomePage } from '@/pages/HomePage';
```

## Рекомендации по именованию

1. **Компоненты**: PascalCase (`WorkoutCard.tsx`)
2. **Хуки**: camelCase с префиксом `use` (`useWorkouts.ts`)
3. **Утилиты**: camelCase (`formatDate.ts`)
4. **Типы**: PascalCase (`Workout`, `WorkoutFormData`)
5. **Константы**: UPPER_SNAKE_CASE (`API_BASE_URL`)

## Структура компонента

```typescript
// WorkoutCard/WorkoutCard.tsx
import { Workout } from '../types';

interface WorkoutCardProps {
  workout: Workout;
  onEdit?: (id: string) => void;
}

export function WorkoutCard({ workout, onEdit }: WorkoutCardProps) {
  // ...
}

// WorkoutCard/index.ts
export { WorkoutCard } from './WorkoutCard';
```

## Redux структура

### Store slice в feature
```typescript
// features/workouts/store/workoutsSlice.ts
import { createSlice } from '@reduxjs/toolkit';

const workoutsSlice = createSlice({
  name: 'workouts',
  initialState: { /* ... */ },
  reducers: { /* ... */ },
});

export const { actions, reducer } = workoutsSlice;
```

### RTK Query API в feature
```typescript
// features/workouts/api/workoutsApi.ts
import { baseApi } from '@/shared/api/baseApi';

export const workoutsApi = baseApi.injectEndpoints({
  endpoints: (builder) => ({
    getWorkouts: builder.query<Workout[], void>({
      query: () => 'workouts',
    }),
  }),
});
```

## Преимущества такой структуры

1. **Масштабируемость** - легко добавлять новые features
2. **Изоляция** - изменения в одном feature не влияют на другие
3. **Переиспользование** - shared компоненты доступны везде
4. **Читаемость** - понятно, где что находится
5. **Тестируемость** - легко тестировать изолированные модули
6. **Поддержка** - проще найти и исправить баги

## Миграция с текущей структуры

1. Создать директории `features/`, `shared/`, `pages/`
2. Переместить существующий код в соответствующие модули
3. Обновить импорты
4. Настроить алиасы в `rsbuild.config.ts` и `tsconfig.json`
