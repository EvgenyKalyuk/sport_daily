# Feature-Sliced Design (FSD) - Подробное руководство

## Что такое Feature-Sliced Design?

**Feature-Sliced Design (FSD)** — это архитектурная методология для организации кода фронтенд приложений. Она помогает создавать масштабируемые, поддерживаемые и предсказуемые проекты.

FSD был разработан специально для фронтенд разработки и решает проблемы:
- Хаотичной организации кода
- Сложности масштабирования
- Невозможности переиспользования компонентов
- Сложности онбординга новых разработчиков

## Основные концепции

### 1. Слои (Layers)

FSD делит код на **7 слоёв**, каждый из которых имеет своё назначение и правила использования:

```
app/          # Инициализация приложения
pages/        # Страницы приложения
widgets/      # Составные блоки интерфейса
features/     # Бизнес-функциональность
entities/     # Бизнес-сущности
shared/       # Переиспользуемый код
```

#### Иерархия слоёв (от верхнего к нижнему):

```
app
  └── pages
      └── widgets
          └── features
              └── entities
                  └── shared
```

**Правило**: Слои могут импортировать только из слоёв, расположенных **ниже** в иерархии.

### 2. Слайсы (Slices)

Внутри каждого слоя код делится на **слайсы** — логические модули, которые группируют связанную функциональность.

Например, в слое `features`:
```
features/
  ├── auth/          # слайс аутентификации
  ├── workouts/      # слайс тренировок
  └── statistics/    # слайс статистики
```

### 3. Сегменты (Segments)

Внутри каждого слайса код делится на **сегменты** — типы файлов по их назначению:

```
features/workouts/
  ├── api/           # сегмент API
  ├── ui/            # сегмент UI компонентов
  ├── model/         # сегмент бизнес-логики (store, типы)
  └── lib/           # сегмент утилит
```

## Детальное описание слоёв

### 📱 `app/` — Инициализация приложения

**Назначение**: Настройка и инициализация приложения.

**Содержит**:
- Конфигурацию провайдеров (Redux, Router, Theme и т.д.)
- Настройку роутинга
- Глобальные стили
- Инициализацию store

**Примеры файлов**:
```
app/
  ├── providers.tsx      # Провайдеры (Redux, Router)
  ├── store.ts          # Redux store
  ├── router.tsx        # Конфигурация роутинга
  └── styles/           # Глобальные стили
```

**Правила**:
- ✅ Импортирует из всех слоёв ниже
- ❌ Не содержит бизнес-логику
- ❌ Не содержит UI компоненты (кроме обёрток провайдеров)

**Пример**:
```typescript
// app/providers.tsx
import { Provider } from 'react-redux';
import { BrowserRouter } from 'react-router-dom';
import { store } from './store'; // ✅ из того же слоя

export function Providers({ children }) {
  return (
    <Provider store={store}>
      <BrowserRouter>
        {children}
      </BrowserRouter>
    </Provider>
  );
}
```

---

### 📄 `pages/` — Страницы приложения

**Назначение**: Композиция страниц из виджетов и features.

**Содержит**:
- Компоненты страниц
- Композицию виджетов и features для конкретной страницы

**Примеры файлов**:
```
pages/
  ├── HomePage/
  │   ├── HomePage.tsx
  │   └── index.ts
  ├── WorkoutsPage/
  │   └── WorkoutsPage.tsx
  └── ProfilePage/
      └── ProfilePage.tsx
```

**Правила**:
- ✅ Импортирует из `widgets`, `features`, `entities`, `shared`
- ❌ Не содержит бизнес-логику (только композицию)
- ❌ Не импортирует из `app` и других `pages`

**Пример**:
```typescript
// pages/WorkoutsPage/WorkoutsPage.tsx
import { WorkoutList } from '@/widgets/WorkoutList'; // ✅ из widgets
import { CreateWorkoutButton } from '@/features/workouts'; // ✅ из features
import { Layout } from '@/shared/ui/Layout'; // ✅ из shared

export function WorkoutsPage() {
  return (
    <Layout>
      <WorkoutList />
      <CreateWorkoutButton />
    </Layout>
  );
}
```

---

### 🧩 `widgets/` — Составные блоки интерфейса

**Назначение**: Сложные составные компоненты, объединяющие несколько features.

**Содержит**:
- Виджеты, которые используют несколько features
- Сложные UI блоки (дашборды, формы с множеством полей)

**Примеры файлов**:
```
widgets/
  ├── WorkoutCalendar/
  │   ├── WorkoutCalendar.tsx
  │   └── index.ts
  ├── StatsDashboard/
  │   └── StatsDashboard.tsx
  └── UserProfileCard/
      └── UserProfileCard.tsx
```

**Правила**:
- ✅ Импортирует из `features`, `entities`, `shared`
- ❌ Не импортирует из `pages`, `app`, других `widgets`
- ⚠️ Используется только для сложных составных компонентов

**Пример**:
```typescript
// widgets/StatsDashboard/StatsDashboard.tsx
import { WorkoutChart } from '@/features/workouts'; // ✅ из features
import { UserStats } from '@/features/statistics'; // ✅ из features
import { Card } from '@/shared/ui/Card'; // ✅ из shared

export function StatsDashboard() {
  return (
    <Card>
      <UserStats />
      <WorkoutChart />
    </Card>
  );
}
```

**Когда использовать widgets?**
- Компонент использует **несколько features** одновременно
- Компонент достаточно сложный и самодостаточный
- Компонент может быть переиспользован на разных страницах

**Когда НЕ использовать widgets?**
- Простой компонент → используйте `shared/ui`
- Компонент относится к одному feature → используйте `features/[name]/ui`

---

### ⚡ `features/` — Бизнес-функциональность

**Назначение**: Бизнес-функции, которые может выполнять пользователь.

**Содержит**:
- Функциональность, которую пользователь может инициировать
- UI компоненты для конкретной функции
- Бизнес-логику функции
- API запросы для функции

**Примеры файлов**:
```
features/
  ├── auth/
  │   ├── api/              # API запросы
  │   ├── ui/               # UI компоненты
  │   │   ├── LoginForm/
  │   │   └── LogoutButton/
  │   ├── model/            # Store, типы, логика
  │   │   ├── authSlice.ts
  │   │   └── types.ts
  │   └── lib/              # Утилиты feature
  │
  ├── workouts/
  │   ├── api/
  │   ├── ui/
  │   ├── model/
  │   └── lib/
  │
  └── statistics/
      ├── api/
      ├── ui/
      └── model/
```

**Правила**:
- ✅ Импортирует из `entities`, `shared`
- ❌ Не импортирует из `pages`, `widgets`, `app`
- ❌ Не импортирует из других features (изоляция)
- ✅ Может использовать несколько entities

**Пример**:
```typescript
// features/workouts/ui/CreateWorkoutForm.tsx
import { Workout } from '@/entities/workout'; // ✅ из entities
import { Button } from '@/shared/ui/Button'; // ✅ из shared
import { useCreateWorkout } from '../api/useCreateWorkout'; // ✅ из того же feature

export function CreateWorkoutForm() {
  const createWorkout = useCreateWorkout();
  // ...
}
```

**Что относится к features?**
- ✅ Действия пользователя: "создать тренировку", "войти в систему", "поделиться результатом"
- ✅ Формы и кнопки действий
- ✅ Бизнес-процессы

**Что НЕ относится к features?**
- ❌ Простые UI компоненты → `shared/ui`
- ❌ Данные и их структура → `entities`
- ❌ Общие утилиты → `shared/lib`

---

### 🏗️ `entities/` — Бизнес-сущности

**Назначение**: Бизнес-сущности приложения (данные и их представление).

**Содержит**:
- Модели данных (типы, интерфейсы)
- Компоненты для отображения сущностей
- Логика работы с сущностями (без бизнес-процессов)

**Примеры файлов**:
```
entities/
  ├── workout/
  │   ├── model/            # Типы, интерфейсы
  │   │   └── types.ts
  │   ├── ui/               # Компоненты сущности
  │   │   ├── WorkoutCard/
  │   │   └── WorkoutListItem/
  │   └── api/              # API для работы с сущностью
  │       └── workoutApi.ts
  │
  ├── user/
  │   ├── model/
  │   ├── ui/
  │   └── api/
  │
  └── exercise/
      ├── model/
      └── ui/
```

**Правила**:
- ✅ Импортирует только из `shared`
- ❌ Не импортирует из других слоёв выше
- ❌ Не содержит бизнес-процессы (это features)

**Пример**:
```typescript
// entities/workout/model/types.ts
export interface Workout {
  id: string;
  name: string;
  date: string;
  exercises: Exercise[];
}

// entities/workout/ui/WorkoutCard/WorkoutCard.tsx
import { Workout } from '../../model/types'; // ✅ из того же entity
import { Card } from '@/shared/ui/Card'; // ✅ из shared

export function WorkoutCard({ workout }: { workout: Workout }) {
  return <Card>{workout.name}</Card>;
}
```

**Что относится к entities?**
- ✅ Сущности предметной области: User, Workout, Exercise, Product, Order
- ✅ Компоненты для отображения сущности (без действий)
- ✅ Типы и модели данных

**Разница между entities и features:**

| entities | features |
|----------|----------|
| "Что это?" (Workout) | "Что можно сделать?" (создать тренировку) |
| Данные и их отображение | Действия и процессы |
| WorkoutCard (показать) | CreateWorkoutForm (создать) |

---

### 🔧 `shared/` — Переиспользуемый код

**Назначение**: Переиспользуемый код, не зависящий от бизнес-логики.

**Содержит**:
- UI компоненты (Button, Input, Modal)
- Утилиты (formatDate, validateEmail)
- Хуки (useDebounce, useLocalStorage)
- API утилиты (baseApi, interceptors)
- Константы

**Примеры файлов**:
```
shared/
  ├── ui/                   # UI компоненты
  │   ├── Button/
  │   ├── Input/
  │   ├── Modal/
  │   └── Layout/
  │
  ├── lib/                  # Утилиты
  │   ├── utils.ts
  │   ├── format.ts
  │   └── validation.ts
  │
  ├── api/                  # API утилиты
  │   └── baseApi.ts
  │
  ├── hooks/                # Переиспользуемые хуки
  │   ├── useDebounce.ts
  │   └── useLocalStorage.ts
  │
  └── config/               # Конфигурация
      └── constants.ts
```

**Правила**:
- ✅ Не импортирует из других слоёв (самый нижний слой)
- ✅ Может импортировать только внешние библиотеки
- ✅ Не содержит бизнес-логику

**Пример**:
```typescript
// shared/ui/Button/Button.tsx
import { type ButtonHTMLAttributes } from 'react'; // ✅ внешняя библиотека

export function Button(props: ButtonHTMLAttributes<HTMLButtonElement>) {
  return <button {...props} />;
}

// shared/lib/format.ts
export function formatDate(date: Date): string {
  return date.toLocaleDateString();
}
```

---

## Сегменты внутри слайсов

Каждый слайс может содержать следующие **сегменты**:

### `ui/` — UI компоненты
Компоненты интерфейса для данного слайса.

### `api/` — API запросы
Запросы к серверу, RTK Query endpoints.

### `model/` — Бизнес-логика
- Store slices (Redux)
- Типы и интерфейсы
- Бизнес-логика

### `lib/` — Утилиты
Утилиты, специфичные для данного слайса.

### `config/` — Конфигурация
Конфигурация слайса (константы, настройки).

**Пример структуры feature:**
```
features/workouts/
  ├── api/
  │   └── workoutsApi.ts
  ├── ui/
  │   ├── WorkoutList/
  │   └── CreateWorkoutForm/
  ├── model/
  │   ├── workoutsSlice.ts
  │   └── types.ts
  └── lib/
      └── calculateTotalDuration.ts
```

---

## Правила импортов

### Основное правило

**Слои могут импортировать только из слоёв, расположенных ниже в иерархии.**

```
app          ❌ не импортирует ниоткуда (кроме внешних библиотек)
  └── pages  ✅ импортирует из widgets, features, entities, shared
      └── widgets  ✅ импортирует из features, entities, shared
          └── features  ✅ импортирует из entities, shared
              └── entities  ✅ импортирует из shared
                  └── shared  ✅ импортирует только внешние библиотеки
```

### Примеры правильных импортов

```typescript
// ✅ pages/WorkoutsPage.tsx
import { WorkoutList } from '@/widgets/WorkoutList'; // widgets
import { CreateWorkoutButton } from '@/features/workouts'; // features
import { WorkoutCard } from '@/entities/workout'; // entities
import { Button } from '@/shared/ui/Button'; // shared

// ✅ features/workouts/ui/CreateWorkoutForm.tsx
import { Workout } from '@/entities/workout'; // entities
import { Button } from '@/shared/ui/Button'; // shared

// ✅ entities/workout/ui/WorkoutCard.tsx
import { Card } from '@/shared/ui/Card'; // shared

// ✅ shared/ui/Button.tsx
import { type ButtonHTMLAttributes } from 'react'; // внешняя библиотека
```

### Примеры неправильных импортов

```typescript
// ❌ features/workouts/ui/CreateWorkoutForm.tsx
import { WorkoutsPage } from '@/pages/WorkoutsPage'; // нельзя импортировать из pages
import { WorkoutCalendar } from '@/widgets/WorkoutCalendar'; // нельзя импортировать из widgets
import { LoginForm } from '@/features/auth'; // нельзя импортировать из других features

// ❌ entities/workout/ui/WorkoutCard.tsx
import { CreateWorkoutForm } from '@/features/workouts'; // нельзя импортировать из features
import { WorkoutList } from '@/widgets/WorkoutList'; // нельзя импортировать из widgets

// ❌ shared/ui/Button.tsx
import { Workout } from '@/entities/workout'; // нельзя импортировать из entities
```

### Исключения

1. **Импорт из того же слоя/слайса** — всегда разрешён
   ```typescript
   // ✅ features/workouts/ui/CreateWorkoutForm.tsx
   import { useCreateWorkout } from '../api/useCreateWorkout';
   ```

2. **Импорт типов из entities в features** — разрешён для типизации
   ```typescript
   // ✅ features/workouts/api/workoutsApi.ts
   import type { Workout } from '@/entities/workout/model/types';
   ```

---

## Public API слайсов

Каждый слайс должен иметь **публичный API** через `index.ts` файл.

### Правило: экспортируйте только необходимое

```typescript
// features/workouts/index.ts
// ✅ Публичный API слайса
export { CreateWorkoutForm } from './ui/CreateWorkoutForm';
export { useGetWorkoutsQuery } from './api/workoutsApi';
export type { Workout } from './model/types';

// ❌ Не экспортируйте внутренние детали
// export { calculateTotalDuration } from './lib/calculateTotalDuration';
```

### Использование публичного API

```typescript
// pages/WorkoutsPage.tsx
import { CreateWorkoutForm, useGetWorkoutsQuery } from '@/features/workouts';
// ✅ Используем публичный API, не внутренние файлы
```

---

## Примеры организации

### Пример 1: Простая страница со списком

```
pages/WorkoutsPage/
  └── WorkoutsPage.tsx
      ├── импортирует WorkoutList из widgets
      └── импортирует CreateWorkoutButton из features/workouts

widgets/WorkoutList/
  └── WorkoutList.tsx
      ├── импортирует WorkoutCard из entities/workout
      └── импортирует Card из shared/ui

entities/workout/
  ├── model/types.ts
  └── ui/WorkoutCard/
      └── WorkoutCard.tsx
          └── импортирует Card из shared/ui

features/workouts/
  └── ui/CreateWorkoutButton/
      └── CreateWorkoutButton.tsx
          ├── импортирует Button из shared/ui
          └── импортирует Workout из entities/workout
```

### Пример 2: Сложный виджет с несколькими features

```
widgets/StatsDashboard/
  └── StatsDashboard.tsx
      ├── импортирует WorkoutChart из features/workouts
      ├── импортирует UserStats из features/statistics
      └── импортирует Card из shared/ui

features/workouts/
  └── ui/WorkoutChart/
      └── WorkoutChart.tsx
          ├── импортирует Workout из entities/workout
          └── импортирует Chart из shared/ui

features/statistics/
  └── ui/UserStats/
      └── UserStats.tsx
          ├── импортирует User из entities/user
          └── импортирует Card из shared/ui
```

---

## Преимущества FSD

### 1. Масштабируемость
- Легко добавлять новые features без влияния на существующие
- Чёткая структура помогает находить нужный код

### 2. Изоляция
- Features не зависят друг от друга
- Изменения в одном feature не ломают другие

### 3. Переиспользование
- Shared компоненты доступны везде
- Entities можно использовать в разных features

### 4. Понятность
- Чёткая иерархия слоёв
- Легко понять, где что находится

### 5. Тестируемость
- Изолированные модули легко тестировать
- Чёткие зависимости упрощают моки

### 6. Онбординг
- Новым разработчикам проще разобраться
- Стандартизированная структура

---

## Частые вопросы

### Q: Когда использовать widgets, а когда features?

**A:** 
- **widgets** — когда компонент объединяет **несколько features**
- **features** — когда компонент относится к **одной бизнес-функции**

Пример:
- `StatsDashboard` (widget) — использует features/workouts и features/statistics
- `CreateWorkoutForm` (feature) — относится только к созданию тренировки

### Q: Можно ли импортировать из features в другие features?

**A:** Нет, features должны быть изолированы. Если нужна общая логика:
- Вынесите в `entities` (если это данные)
- Вынесите в `shared` (если это утилиты)

### Q: Где хранить типы?

**A:** 
- Типы сущностей → `entities/[name]/model/types.ts`
- Типы feature → `features/[name]/model/types.ts`
- Общие типы → `shared/types/`

### Q: Как организовать API запросы?

**A:**
- RTK Query base API → `shared/api/baseApi.ts`
- Endpoints для сущности → `entities/[name]/api/[name]Api.ts`
- Endpoints для feature → `features/[name]/api/[name]Api.ts`

### Q: Нужно ли всегда использовать все слои?

**A:** Нет, используйте только необходимые:
- Маленькое приложение: `app`, `pages`, `shared`, `features`
- Среднее приложение: + `entities`, `widgets`

---

## Миграция на FSD

### Шаг 1: Создайте структуру директорий
```
src/
  ├── app/
  ├── pages/
  ├── widgets/ (опционально)
  ├── features/
  ├── entities/
  └── shared/
```

### Шаг 2: Переместите существующий код
- UI компоненты → `shared/ui/`
- Утилиты → `shared/lib/`
- Store → `app/store.ts`
- Redux slices → `features/[name]/model/` или `entities/[name]/model/`

### Шаг 3: Разбейте на features
Определите бизнес-функции и создайте для каждой feature:
- `features/auth/`
- `features/workouts/`
- `features/statistics/`

### Шаг 4: Выделите entities
Определите бизнес-сущности:
- `entities/workout/`
- `entities/user/`
- `entities/exercise/`

### Шаг 5: Создайте страницы
Соберите страницы из widgets и features:
- `pages/WorkoutsPage/`
- `pages/StatisticsPage/`

### Шаг 6: Настройте алиасы
Обновите `tsconfig.json` и `rsbuild.config.ts` с алиасами для всех слоёв.

---

## Дополнительные ресурсы

- [Официальный сайт FSD](https://feature-sliced.design/)
- [Документация FSD](https://feature-sliced.design/docs)
- [Примеры проектов](https://github.com/feature-sliced/documentation/tree/master/examples)

---

## Резюме

FSD — это методология, которая помогает:
- ✅ Организовать код предсказуемым образом
- ✅ Масштабировать проект без хаоса
- ✅ Изолировать функциональность
- ✅ Переиспользовать код эффективно
- ✅ Упростить онбординг новых разработчиков

Следуйте правилам импортов, используйте правильные слои для правильных целей, и ваш код станет чище и поддерживаемее!
